// Source of truth for WMP tracks: auto-generated from public/audio/.
// Drop new audio files there, then run `pnpm build:tracks` (or any build).
//
// Title / author are parsed heuristically from filenames — see
// scripts/build-track-index.mjs for the rules.
export { TRACKS } from "./_tracks-bundled";
